<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.JustMessages">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Just Messages"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AmityNewsLetter">
        <activity android:name="com.example.JustMessages.receiveActivity"></activity>
        <activity android:name="com.example.JustMessages.sendActivity" />
        <activity android:name="com.example.JustMessages.secondActivity" />
        <activity android:name="com.example.JustMessages.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>